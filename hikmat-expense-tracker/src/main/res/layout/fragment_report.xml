<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.hikmat.expense.ui.report.ReportViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <TextView
            android:id="@+id/report_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Weekly Expenditure Report"
            android:textSize="24sp"
            android:textStyle="bold" />

        <RecyclerView
            android:id="@+id/recycler_view_expenses"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/report_title"
            android:layout_marginTop="16dp"
            app:layoutManager="LinearLayoutManager" />

        <TextView
            android:id="@+id/total_expenditure"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/recycler_view_expenses"
            android:layout_marginTop="16dp"
            android:text="@{`Total Expenditure: $${viewModel.totalExpenditure}`}"
            android:textSize="18sp" />

    </RelativeLayout>
</layout>